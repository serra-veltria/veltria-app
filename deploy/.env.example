# Production Environment Variables
# Copy to /opt/veltria/.env and fill in secure values

# JWT Authentication (min 32 characters - generate with: openssl rand -base64 48)
JWT_SECRET=change-this-to-secure-random-string
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=https://app.veltria.ai
